# Forgot Password → OTP ফ্লো (যা যা পরিবর্তন করা হয়েছে)

## সমস্যা কী ছিল
আগে "Forgot Password" এ ক্লিক করলে Supabase একটা **magic link** মেইল করত
(`resetPasswordForEmail`)। কিন্তু সেই লিংকে ক্লিক করলে সাইটে কোনো "নতুন পাসওয়ার্ড
বসান" পেজ ছিল না — তাই ইউজার সরাসরি হোমপেজে (স্ক্রিনশটে যেটাকে "img এর পেজ" বলা
হয়েছে) চলে যেত এবং পাসওয়ার্ড রিসেট করার আর কোনো উপায় থাকত না।

## এখন কী করা হয়েছে
পুরো ফ্লো-টা **OTP-ভিত্তিক** করে দেওয়া হয়েছে, ঠিক যেভাবে চাওয়া হয়েছিল:

1. Login পেজে Password এর পাশে এখন **"Forgot password?"** লিংক আছে (`index.html`)।
2. ক্লিক করলে ৩ ধাপের একটা ফর্ম খোলে:
   - **ধাপ ১:** Username/Email দিলে Supabase একটা **6-digit code** মেইল করে।
   - **ধাপ ২:** সেই কোড বসিয়ে "Verify Code" দিলে Supabase-এ যাচাই হয়
     (`auth.verifyOtp(..., type: 'recovery')`)।
   - **ধাপ ৩:** verify সফল হলে সাথে সাথে **New Password** ও **Repeat New Password**
     বক্স আসে। এখানে লিখে সাবমিট করলে `auth.updateUser({ password })` দিয়ে
     **আসল Supabase account password** সেট হয়ে যায় — এটা লোকাল কিছু না,
     সত্যিকারের account password চেঞ্জ হয়।
   - সফল হলে ইউজার সাথে সাথে সাইন-ইন অবস্থায় থাকে (আলাদা করে আবার লগইন করা লাগে না)।
3. মাঝপথে (কোড verify হওয়ার পর কিন্তু নতুন পাসওয়ার্ড সেট করার আগে) কেউ মোডাল বন্ধ
   করে দিলে, নিরাপত্তার জন্য সেই সাময়িক session অটো sign-out হয়ে যায়।
4. "Resend code" এ ৪৫ সেকেন্ডের cooldown আছে, বারবার ক্লিক করে স্প্যাম ঠেকাতে।

### পরিবর্তিত ফাইল
- `index.html` — Forgot-password লিংক + নতুন ৩-ধাপের ফর্ম UI
- `script.js` — নতুন ফাংশনগুলো (`openForgotPasswordFlow`, `handleSendResetOtp`,
  `handleVerifyResetOtp`, `handleSubmitNewPassword` ইত্যাদি); পুরনো
  `handleForgotPassword` (magic-link) ফাংশনটা বাদ দেওয়া হয়েছে
- `style.css` — OTP ইনপুট বক্সের স্টাইল + ছোট কিছু CSS

## ⚠️ আপনাকে যেটা Supabase Dashboard থেকে করতে হবে (কোডে করা সম্ভব না)
এই কোড `resetPasswordForEmail()` কল করলে Supabase তার **"Reset Password"** ইমেইল
টেমপ্লেট পাঠায়। ডিফল্ট টেমপ্লেটে (আপনার স্ক্রিনশটে যেটা দেখা গেছে) শুধু একটা লিংক
থাকে, কোনো ৬-সংখ্যার কোড থাকে না — তাই টেমপ্লেট এডিট না করলে মেইলে কখনোই কোনো OTP
কোড আসবে না এবং এই নতুন ফ্লো কাজ করবে না।

**করণীয়:**
1. Supabase Dashboard → **Authentication → Email Templates → Reset Password** এ যান
2. টেমপ্লেটের ভেতর যেকোনো জায়গায় `{{ .Token }}` ভেরিয়েবলটা বসান, যেমন:
   ```html
   <p>আপনার পাসওয়ার্ড রিসেট কোড: <strong>{{ .Token }}</strong></p>
   ```
3. (ঐচ্ছিক) পুরনো "Reset password" লিংকটা (`{{ .ConfirmationURL }}`) টেমপ্লেট থেকে
   পুরোপুরি সরিয়ে দিতে পারেন, যেহেতু এখন লিংকের বদলে কোড দিয়েই কাজ হবে — লিংক
   রেখে দিলেও সমস্যা নেই, ইউজার শুধু সেটা ব্যবহার করবে না।
4. Save করুন।

এই একটা ধাপ ছাড়া কোডের বাকি সব ঠিকভাবে বসানো থাকলেও OTP মেইলে আসবে না।
